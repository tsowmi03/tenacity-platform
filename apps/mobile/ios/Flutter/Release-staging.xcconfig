#include "Release.xcconfig"

// Staging flavor. Distinct bundle id so it installs alongside production and
// can never be submitted to the App Store by accident.
FLAVOR = staging
PRODUCT_BUNDLE_IDENTIFIER = com.tenacityTutoring.tenacity.staging
APP_DISPLAY_NAME = Tenacity STG
